{% extends "base.html.twig" %}
{% block title %}Ajouter un projet{% endblock %}
{% block body %}
{% set nav = 'servi' %}
{% include "partials/menu.html.twig" %}
 <h1>Ajouter un service</h1>
<div class="cadre form service">
 <div class="link2">
        <a href="{{path('admin_services')}}"><button class="btn btn-success retour">Retour</button></a>
        </div>
            {{form_start(myform)}}
                {{form_widget(myform)}}
                <button type="submit" class="btn btn-success">Enregistrer</button>
            {{form_end(myform)}}
</div>
{% endblock %}